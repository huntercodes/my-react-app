const Education = ({ headerTitle, description }) => {
    return (
        <div>
            <h2>{headerTitle}</h2>
            <p>{description}</p>
        </div>
    );
};

export default Education;